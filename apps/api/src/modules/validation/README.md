# Validation Module (reserved)

The deterministic validation & trust engine lands here (backed by `@aide/validation`):
schema validation of AI output, field validators, business rules (skip when no
email AND no phone), enum enforcement, repair engine, and confidence scoring.
AI output is a proposal, not a fact — this module is the gatekeeper.
