# AI Module (reserved)

The AI extraction engine lands here: provider adapter (OpenAI default, swappable),
token-aware batch builder, prompt assembly (via `@aide/prompt`), response parsing,
JSON repair, and the retry ladder. Nothing in this module exists yet by design —
the foundation phase is deliberately AI-free.
